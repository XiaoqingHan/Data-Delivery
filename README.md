# Data-Delivery

## The scipts are written in shell and python, and run in the Linux environment.

Step1: Get the all data of auto flows from the provided api (step1_input.sh) and generate the out.txt file.

Step2: Get the info from filtered data (step2_data_deliv.py) and generate the daily_merge.xlsx file.
